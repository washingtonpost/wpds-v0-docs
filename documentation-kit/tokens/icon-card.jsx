import React from 'react'
import { ReactSVG } from 'react-svg';


export default function IconCard(props) {
    

    return (
        <div>
            {/* <ReactSVG src={path}
                beforeInjection={(svg) => {
                    svg.classList.add(`${props.svgClass}`);
                    svg.setAttribute('style', `width:${props.size}px`)
                    svg.setAttribute('style', `height:${props.size}px`)
                    svg.setAttribute('style', `fill:${color}`)
                }}
            /> */}
        </div>
    )
}
