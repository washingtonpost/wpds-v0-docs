---
order: 2
title: Base size
description:
---

Base size token refers to the value that controls the value scale of `size`, `space`, `font-size`, and `radius`

<BR />

<Table
  headers={["Name", "Value", "Description"]}
  data={[
    {
      name: "baseSize",
      value: "16px",
      description:
        "Root value that defines tokens such as space, size, radius, and font size",
    },
  ]}
/>
