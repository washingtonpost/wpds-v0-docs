---
title: Input Password
description:
component: InputPassword
status: "Draft"
---

## Anatomy

<Container caption="Note: Image is not  to scale" css={{ padding: "$150" }}>
  <InlineSVG
    path="/img/components/input-password/anatomy.svg"
    width={555}
    height={196}
  />
</Container>

1. Password value
2. Password Label
3. Input container
4. Button Icon

---

## Options

### Show/Hide

Toggling the button icon will show or hide the password value

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword />;
}
```

---

## Behavior

### Show/Hide

The password input by default renders the password hidden.

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword />;
}
```

### Focus

Click password input to demostrate the focus state.

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword />;
}
```

### Error

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword error={true} />;
}
```

### Disabled

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword disabled />;
}
```

### Text Overflow

```jsx withPreview
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return (
    <InputPassword value="A really really really really really long password" />
  );
}
```

---

## Guidance

### Ensure the width of the field appropriately sized

Password lengths can vary based on user preference; ensure any reasonably long password will be fully visible within the input.

```jsx withPreview isGuide="error"
import { InputPassword } from "@washingtonpost/wpds-ui-kit";

export default function Example() {
  return <InputPassword css={{ width: "60px" }} defaultValue="123456789" />;
}
```

---

## API Reference

{/* <PropsTable props={propsTable} /> */}
