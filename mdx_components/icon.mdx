---
title: Icon
description: Size an icon using our system. Also add a label that will be announced by screen readers.
component: icon
---

## Anatomy

<Container caption="Icon component shown with an Add icon asset. Inspect the HTML to see the props. Or use a screen reader to read the label.">
	<EmbedStory componentName="icon" />
</Container>

### Import the component

```jsx
import { Icon } from "@washingtonpost/wpds-ui-kit";

export default () => <Icon />;
```

---

## Options

<Container>
	<EmbedControls
		componentName="icon"
		css={{
			marginBottom: "$050",
		}}
	/>
</Container>

### Size

The `size` can be 16px, 24px, or 32px. The size sets the width and height of the SVG.

```jsx
// preview
import { Icon } from "@washingtonpost/wpds-ui-kit";
import Add from "@washingtonpost/wpds-assets/asset/add";

export default function App() {
	return (
		<Icon label="Add to List" size="16">
			<Add />
		</Icon>
	);
}
```

### Label

The `label` is required for screen readers. It is used to describe the icon to screen readers. It is also used as the `aria-label` for the icon. It is recommended to use a short, descriptive label. For example, "Add to List".

```jsx
// preview
import { theme, Icon } from "@washingtonpost/wpds-ui-kit";
import Add from "@washingtonpost/wpds-assets/asset/add";

export default function App() {
	return (
		<Icon label="Add to List" size="16">
			<Add />
		</Icon>
	);
}
```

---

## Behavior

<Container>Example </Container>

---

## Specs

<Container>
	<EmbedDocsPage componentName="icon" />
</Container>

---

## Accessibility

---
